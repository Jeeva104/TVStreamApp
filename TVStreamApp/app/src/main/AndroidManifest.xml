<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for streaming video and loading thumbnails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Declare Leanback software feature as OPTIONAL so the APK is also
        installable on phones (required=false means it can run without it).
        Declare touchscreen as NOT required so TV devices (no touchscreen) accept the APK.
    -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVStream"
        tools:targetApi="31">

        <!--
            MainActivity serves as the entry point for BOTH TV and Phone.
            - LEANBACK_LAUNCHER: shows app in the Android TV home launcher
            - LAUNCHER: shows app in the standard phone app drawer
            Both intents reference the same Activity – single APK approach.
        -->
        <activity
            android:name=".ui.home.MainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.TVStream">

            <!-- Phone / standard launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android TV launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            PlayerActivity – fullscreen video playback.
            noHistory = true ensures it's removed from back-stack on exit (clean UX on TV).
            screenOrientation = landscape for immersive TV playback.
        -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.TVStream.Player" />

    </application>

</manifest>
